Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0
PyJWT==2.8.0
cryptography>=41.0.0
supabase==2.6.0
httpx==0.27.2
openai==1.59.2
sentry-sdk[flask]==2.19.0
resend==2.4.0
stripe==11.1.1
numpy>=1.24.0
imageio-ffmpeg
boto3>=1.34.0
# Audio DSP — richer feature extraction (MFCC, spectral_*, pyin pitch,
# onset detection). Pulls scipy + numba + soundfile + audioread.
# ~150-200MB image bloat + ~5-10s numba JIT cold start on first call
# (warmed at worker boot via gunicorn_conf.py post_worker_init).
librosa==0.10.2
soundfile>=0.12.0
# Slide-deck parsing (UX Wave 4, PDF-only): pypdf extracts per-slide text from
# the uploaded PDF. Lazy-imported in services/deck_parser.py. (PPTX is not
# converted server-side — users export to PDF; LibreOffice was dropped.)
pypdf==5.1.0
# dev-tasks PDF export: the backlog with each screenshot embedded under its task.
# A rich-text paste drops data: URI images in many targets and a .md with data:
# URIs renders in almost nothing, so a PDF is the only format where the images
# reliably travel. Lazy-imported in services/dev_tasks.py::_render_pdf, and a
# missing/failed import degrades to the ZIP export rather than breaking Export.
# Pulls Pillow (manylinux wheel, no system libs) for non-JPEG images.
reportlab==5.0.0
# life-panel strategy export (#11): an EDITABLE Word file alongside the PDF,
# so the strategy can be reviewed and marked up offline. Lazy-imported in
# services/strategy_export.py; a missing/failed import degrades that download
# to markdown rather than breaking the button. Pure-python, no system libs.
python-docx==1.1.2
# Shadow direction classifier (willab Phase 4 / Prompt 1): interpretable
# logistic regression on the 11 acoustic features, served in-process. joblib
# serializes the fitted pipeline → object storage. SHADOW-only; influences
# nothing. Lazy-imported in services/learning_train.py.
scikit-learn==1.5.2
joblib==1.4.2
