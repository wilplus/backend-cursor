# Taskmaster — only source of truth

**APP_DESCRIPTION.md** in this folder is the **only** source of truth for the Homework / Speaking Coach app. It contains the full app description, flow, contracts, APIs, scoring, wheel, storage, what is missing, failure modes, and implementation checklist. No other description or how-to docs; only `.taskmaster/docs/` and code (e.g. `migrations/`, `docs/homework-bff-routes/`) remain.

- **APP_DESCRIPTION.md** — full app spec (read this first).
- **schema.sql** — complete DB schema (single file; update in place only).
- **WHEEL-USE-BFF-URL.md** — wheel = client-side only (real-time metrics, no glow, no BFF).
