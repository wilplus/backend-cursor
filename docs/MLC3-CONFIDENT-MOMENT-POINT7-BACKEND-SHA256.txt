09ac4873e142bd68c682abaf28acb15e4fb0a445aab749ad8b28b47020b1df22  services/data_purge.py
5ff3880f55da07671775de92e279e13e0dca96b615329632b6d6cd88096cbbb9  services/data_purge_registry.py
ab25afecab235eabf97488fa5bf954698d0660bad932dcbb6cdfe1e7105ad069  services/db.py
b40ae9301c2e2712248c4b77314cff27c7342362f7e929cab3edf6b2fe48abb3  services/feedback_data_contract.py
35955687744b693dfb26fe79b79767c54c3a851dabfba4c015790c4537c459c7  services/feedback_language.py
dd6f5eb8b632d5f17e0b9d5915409be75e6802f2f3cffc955afe4f3bc1afdeaa  services/job_queue.py
588e9a03b11dfa6ff1f92e8535f4f45a3eb0032c52081ba4579a2d7e855c73a3  services/mlc3_first_client_feedback.py
a32a213e6d2dd0c20a0bc4d9daee5985802eee650334996268b9797225e61b10  services/mlc3_general_service_monitor.py
6e9894c610fb64f3b00652a171ea698907562a0352614023048374e2c8e8af68  services/pipeline_jobs.py
969647d87ca6be07b395d3be3a70ba5a0b5dedb82a5d90159729ac19807ffe12  services/rooting_coverage.py
8022605ce71926ede78eb546159879143326635835008839976b087b90f2b74f  tests/test_acoustic_kpi.py
caef0a4d2a64dbfb95709850d31861c83b24c65acae9a24d70a3c6280a4db7c7  tests/test_ideal_text_parts.py
7fbf28eb5bf4f764034575309183e06cca24a28caa00a2786d3b6ffb979879ec  tests/test_user_ideal_edit.py
a4bfdffa958538a137d6b58b57c49ecc11d95401663d5f6b162c90decb8f7f0e  tests/test_confident_moment_bundle.py
d54bfcf21c5d446cbf0243830e305eb5d42bf510a6d1ca46e8c4ccada79d0f5d  tests/test_confident_moment_bundle_routes.py
ac2d91c6b987275fd06838f26926324f7bf6947fc297eec23793c1d3c6812aec  tests/test_confident_moment_bundle_security.py
e1d4ac7f2b92c0049fa3d62f6b740338452ab95de1cb3d180c5feabb42b292d7  tests/test_confident_moment_coaching_bundle_postgres.py
5e448ef42d2250dc98bda01cdd72bdbdc34b88ab283cb8725535dd7bcc0b9dab  tests/test_confident_moment_delivery_d37.py
db67fa593b54ccf89476f38fca1431461f2e7b3b04549301f30e9bb9294fc1c7  tests/test_confident_moment_transport_d25.py
2770045773416a54a4269897e2a9c32c90fd0625c43721ddec212087b6dbcf52  tests/test_confident_moment_user_media.py
0430a5068776f1806dc1ecc073b1a2071ff3250e5ba9c3190390e062ced8a402  tests/test_ideal_text_cold_open.py
f4a54c1e0a9dcbdd2c87d7dab154785a9a5926aef41bad8e34c5e3d5d4856f00  tests/test_mlc3_first_client_feedback.py
9d2231724e1b0fe08e5f194a36bbcb1d19254e18db5e63789e4da65ed541c31f  tests/test_mlc3_general_user_service_d4_postgres.py
a467eac99adafc5252dbb9ece243153fe905ea0cb58206893d85d3e87a9d1ddc  tests/test_mlc3_general_service_monitor.py
7e580df98d57b6a7e90146ab508195a8a4016e4acda0681de6242c2684943b27  worker.py
```

## Re-freeze 2026-09-14 — post-audit remediation, Phase 4 (backend)

Re-frozen by the remediation programme's rule ("if a change touches a reviewed
executable file, re-freeze the hashes and say so"). The packet's own rule
("repeat both reviews") is the founder's to apply. Every entry above was
consistent with `main` before Phase 4; the drift is Phase 4's alone:

- `config.py` — audit Q-A5: 17 attributes with no reader deleted; the F1
  flags and tunables added as boot-time Config attributes.
- `services/db.py` — audit Q-A1 step 1: 106 methods with no caller deleted;
  step 2 / Q-A2: 81 F1-table methods carved into three repository modules
  (verbatim, delegates kept) and `new_client()` added.
- `services/feedback_data_contract.py`, `services/pipeline_jobs.py`,
  `routes/v2/explore_ideal_text.py` — audit Q-A5: their environment reads
  now go through Config (same defaults, same parsing).
- `test_ideal_text_parts.py`, `test_user_ideal_edit.py`,
  `tests/test_confident_moment_bundle_routes.py` — audit Q-A3: re-pointed
  from the `routes.v2_routes` façade at the domain modules.
- `routes/v2_routes.py` — audit Q-A3: DELETED. Its 250 re-exports had no
  production importer; the load-bearing import order now lives in
  `routes/v2/__init__.py` (`DOMAIN_MODULES`, `register_domains()`), and the
  application's URL map was verified byte-identical with and without it.
  Its entry is removed rather than hashed.

Migration 0327 and every other entry are untouched.

## Re-freeze 2026-09-14 — post-audit remediation, Phase 5 (backend)

Re-frozen by the same rule. Every entry above was consistent with `main`
before Phase 5; the drift is Phase 5's alone:

- `routes/v2/explore_ideal_text.py` — audit Q-C1: the 901-line `changes`
  block moved to `services/ideal_text_changes.py`, split by stage; the
  route keeps a delegating wrapper (`_tracked_changes_block`) and its GET
  records every optional-read, compose and exposure fallback into one
  `DegradationLog`, served as `degraded` only when non-empty.
- `services/pipeline_jobs.py` — audit Q-C1: the session-recording runner
  creates the run's `DegradationLog` and the job row's `result` carries
  `degraded` (stage names and exception class names only; never numbers).

`services/confident_moment_bundle_repository.py`, migration 0327 and every
other entry are untouched.

## Re-freeze 2026-09-14 — post-audit remediation, Phase 6 (backend)

Re-frozen by the same rule. Every entry above was consistent with `main`
after Phase 5; the drift is Phase 6's alone:

- `routes/v2/explore_ideal_text.py`, `services/db.py` — audit Q-T4
  (commit 1): the import of the per-slide
  selection engine re-pointed from `services.best_presentation` to
  `services.slide_selection` (module renamed; zero behaviour change).
- `services/db.py` — audit Q-A7: `update_coaching_attempt_self_rating` and
  `get_user_kpi_timeline_rows` deleted with the two routes that were their
  only callers.

`services/confident_moment_bundle_repository.py`, migration 0327 and every
other entry are untouched.

## Re-freeze 2026-09-14 — post-audit remediation, Phase 6b (backend)

Re-frozen by the same rule. Every entry above was consistent with `main`
after Phase 6 (#499); the drift is Phase 6b's alone:

- `services/db.py` — audit Q-A6: the ten `coaching_sessions` /
  `coaching_attempts` methods whose only callers were the retired coaching
  lane (`create_coaching_session`, `get_coaching_session`,
  `append_coaching_message`, `update_coaching_stage`,
  `set_coaching_trial_recording`, `v2_get_student_coaching_memory`,
  `insert_coaching_attempt`, `_next_coaching_attempt_number`,
  `list_coaching_attempts_for_snippet`,
  `list_recent_coaching_attempts_for_user`) deleted. The two annotation
  methods the admin routes read stay. No table is touched.
- `config.py` — audit Q-A6: `LEARNER_PROFILE_INJECTION_ENABLED` deleted;
  its only reader was the retired coaching turn's prompt augmentation.

`services/confident_moment_bundle_repository.py`, migration 0327 and every
other entry are untouched.

## Path update 2026-09-14 — audit Q-T9 (backend)

`test_acoustic_kpi.py`, `test_ideal_text_parts.py` and `test_user_ideal_edit.py`
moved from the repository root to `tests/` with every other root test
module (audit Q-T9). Their bytes are unchanged — the three checksums above
are the ones frozen at the base commit; only the paths are new. Every
other entry is untouched.

## Re-freeze 2026-09-15 — live-loop incident fix (backend)

Re-frozen by the same rule. The drift is one method:

- `services/db.py` — `get_ideal_text_document_core_v2` no longer re-raises
  what `read_ideal_text_document_core_v2` or the validator raises; it logs
  the exception with its traceback and falls back to the v1 read
  (`_ideal_text_core_v1_fallback`, served in the v2 envelope shape with an
  `unavailable` overlay), returning `None` — the 404 "pending" — only when
  neither read has a document. This restores what the core GET served
  before #490 for arcs the v2 read cannot serve. The RPC raises (STRICT selects, explicit RAISE)
  for any arc without the Point-7 rows, so since #490 every such read was a
  500 in production; on 2026-09-15 that took the Ideal Text surface down.
  The validator is unchanged: nothing it rejects is ever served.

`services/confident_moment_bundle_repository.py`, migration 0327 and every
other entry are untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, wrapper repointing (backend)

Re-frozen by the same rule. Two pinned files drift, both zero-behavior-
change cleanup: the audit's Q-A2 continuation, repointing `services/db.py`'s
one-line pass-through wrappers at the F1 repositories they already
delegate to, instead of keeping the forwarder.

- `services/db.py` — the `create_recording`, `update_recording`,
  `get_user_recordings`, `get_user_recording_history` and
  `get_recording_attempt` forwarders are deleted. `update_recording` and
  `get_user_recording_history` had zero callers anywhere (production,
  scripts or tests) even before this change — both they and their
  now-orphaned bodies in `services/recording_repository.py` are deleted
  outright, not repointed. The other three had real callers, repointed
  below.
- `services/pipeline_jobs.py` — `db.get_recording_attempt(...)` becomes
  `db.recordings.get_recording_attempt(...)`; the defensive
  `getattr(db, "get_recording_attempt", None)` compatibility lookup in
  `_has_canonical_attempt` now reads `getattr(getattr(db, "recordings",
  None), "get_recording_attempt", None)`.
- `routes/user.py`, `services/training_import.py`,
  `services/lab_recording_persistence.py` — their `db.`/`database.` calls
  now go through `.recordings.` directly (not pinned files; changed
  alongside, not listed above).

No table, migration, route, or response shape changes. Verified by
execution: targeted tests for every touched file green, then the full
gate.

Every other entry is untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, ideal-text family wrapper repointing (backend)

Re-frozen by the same rule. Two pinned files drift, both zero-behavior-
change cleanup: the second of three families in the audit's Q-A2
continuation. `get_coach_arc_ideal_text` (by far the largest caller
surface of the nine ideal-text-family forwarders — dozens of call sites
across the Ideal Text read/write routes) is deferred to its own,
separately-verified PR given its size; the other eight are repointed here.

- `services/db.py` — `delete_ideal_text_block`, `get_canonical_confidence_
  evidence`, `get_ideal_text_block`, `get_latest_ideal_text_save`,
  `insert_ideal_text_save`, `list_ideal_text_blocks`,
  `upsert_coach_arc_ideal_text` and `upsert_ideal_text_block` forwarders
  are deleted. `get_coach_arc_ideal_text`'s own forwarder stays for now.
- `routes/v2/explore_ideal_text.py` — its `db.list_ideal_text_blocks(...)`,
  `db.insert_ideal_text_save(...)` and `db.get_latest_ideal_text_save(...)`
  calls now go through `db.ideal_text.`.
- `routes/v2/coach.py`, `services/master_document.py`,
  `services/ideal_text_variants.py` — their calls to the other five
  methods now go through `db.ideal_text.` / `database.ideal_text.` (not
  pinned files; changed alongside, not listed above).
- Two test fixtures (`tests/test_master_document.py`,
  `tests/test_ideal_text_variants.py`) each have their own hand-rolled
  `_Db` fake implementing these methods directly on itself; both gained a
  one-line `ideal_text` property returning `self`, so the repointed
  production calls resolve to the exact same fake methods. Two
  `patch.object(db, "upsert_coach_arc_ideal_text", ...)` /
  `patch.object(v2_db, ...)` test sites move to patching
  `db.ideal_text` / `v2_db.ideal_text` instead.

No table, migration, route, or response shape changes. Verified by
execution: targeted tests for every touched file green (417 tests across
the ideal-text surfaces), then the full gate.

Every other entry is untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, get_coach_arc_ideal_text (backend)

Re-frozen by the same rule. Four pinned files drift, zero-behavior-change
cleanup: the last of the nine ideal-text-family forwarders, deferred out
of the previous re-freeze given its size (~20 production call sites,
~30 test-mock sites).

- `services/db.py` — the `get_coach_arc_ideal_text` forwarder is deleted.
  Its two internal callers on `DatabaseService` itself
  (`persist_auto_ideal_text`, `verify_ideal_text`) now call
  `self.ideal_text.get_coach_arc_ideal_text(...)`.
- `routes/v2/explore_ideal_text.py` — its eight `db.get_coach_arc_ideal_text(...)`
  calls now go through `db.ideal_text.`.
- `routes/v2/arcs.py`, `routes/v2/coach.py`, `routes/v2/lab_recording.py`,
  `routes/v2/user_sessions.py`, `services/arc_notifications.py`,
  `services/ideal_text_block.py`, `services/ideal_text_changes.py`,
  `services/ideal_text_confirmation.py`,
  `services/ideal_text_core_snapshot.py`, `services/part_acoustics.py`,
  `services/swap_detector.py`, `services/take_review.py` — their calls to
  the same method now go through `db.ideal_text.` / `database.ideal_text.`
  / `self.db.ideal_text.` (not pinned files; changed alongside, not listed
  above). `services/ideal_text_block.py`'s defensive
  `getattr(database, "get_coach_arc_ideal_text", None)` indirection now
  nests through a `getattr(database, "ideal_text", None)` lookup first,
  the same pattern used for `services/pipeline_jobs.py` in the recordings
  family's re-freeze.
- Nine test fixtures (`tests/test_eager_ideal_text.py`,
  `tests/test_ideal_bubble_backfill.py` — two fake classes,
  `tests/test_ideal_text_changes.py`, `tests/test_moment_suggestions.py`,
  `tests/test_pipeline_degradation.py`, `tests/test_take_review.py`) each
  have their own hand-rolled fake DB class implementing this method
  directly on itself; each gained a one-line `ideal_text` property
  returning `self`. Three `SimpleNamespace(...,
  get_coach_arc_ideal_text=lambda a: row)` fakes
  (`tests/test_instant_ideal_text.py`, two in
  `tests/test_single_deliverable.py`) nest the lambda under a
  `SimpleNamespace(ideal_text=...)` instead. `patch.object(db,
  "get_coach_arc_ideal_text", ...)` / `patch.object(v2_db, ...)` /
  `patch.object(route.db, ...)` test sites across eleven files — including
  the two pinned `tests/test_ideal_text_parts.py` and
  `tests/test_user_ideal_edit.py` — move to patching `db.ideal_text` /
  `v2_db.ideal_text` / `route.db.ideal_text` instead. Mock-based
  `database.get_coach_arc_ideal_text.return_value` / `.side_effect` sites
  in `tests/test_ideal_text_block.py` and
  `tests/test_ideal_text_confirmation.py` move to
  `database.ideal_text.get_coach_arc_ideal_text.*`.
- `scripts/route_fence_baseline.json` — re-frozen (down only) in the same
  commit: several `routes/v2/coach.py` and
  `routes/v2/explore_ideal_text.py` functions count fewer direct
  `db.<method>()` calls now that this call routes through `db.ideal_text`,
  and `routes/v2/explore_ideal_text.py:v2_explore_decide_prior_take` /
  `routes/v2/lab_recording.py:v2_retry_recording_ideal_text` dropped out
  of the baseline entirely (now within both thresholds).

The recordings, ideal-text and takes families are now the only remaining
`services/db.py` forwarder groups; this closes the ideal-text family
(nine of nine methods repointed across two PRs).

No table, migration, route, or response shape changes. Verified by
execution: targeted tests for every touched surface green (621 tests
across the ideal-text/coach/arc surfaces), then the full gate.

Every other entry is untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, takes family batch A (backend)

Re-frozen by the same rule. Two pinned files drift, zero-behavior-change
cleanup: Phase 1 step 3 of the post-audit wrapper cleanup, the takes
family — by far the largest of the three `services/db.py` forwarder
groups (66 methods). Split by caller-count into three batches; this is
batch A, the 58 lowest-traffic methods. Four higher-traffic methods
(`get_session_intake_context`, `get_session_slide_transcripts`,
`set_session_intake_context`, `set_session_analysis_state`) and four very
high-traffic methods (`list_user_lab_sessions`,
`v2_list_user_lab_sessions`, `get_arc_sessions`, `v2_get_session`) are
deferred to their own, separately-verified PRs given their size.

- `services/db.py` — 58 takes-family forwarders deleted. Two internal
  callers on `DatabaseService` itself
  (`v2_cleanup_incomplete_sessions`'s two `self.<method>(...)` calls) now
  call `self.takes.<method>(...)`.
- `routes/v2/admin.py`, `routes/v2/coach.py`, `routes/v2/coaching.py`,
  `routes/v2/user_sessions.py`, `scripts/smoke_rlhf_capture.py`,
  `services/conversation_summary.py`,
  `services/lab_recording_persistence.py`, `services/lab_send.py`,
  `services/next_session_icebreaker.py`, `services/pipeline_jobs.py`,
  `services/project_repository.py`,
  `services/recording_transcript_persistence.py`,
  `services/session_metrics.py`, `services/training_import.py` — call
  sites repointed to `db.takes.<method>()` / `database.takes.<method>()`
  / `self.database.takes.<method>()` (only `services/pipeline_jobs.py` is
  itself pinned; the rest changed alongside, not listed above).
- Three hand-rolled test fake DB classes (`test_create_take.py`,
  `test_orphan_sweep_and_concurrency.py`, `test_processing_jobs.py`) each
  gain a one-line `takes` property returning `self`. Two generic
  `_patch_db(attr, fn)` test helpers (`test_coach_video.py`,
  `test_presentation_delete.py`) that `setattr(db, attr, fn)` directly
  now check `db.takes` first (`target = db.takes if hasattr(db.takes,
  attr) else db`) so a takes-family attr is monkey-patched on the right
  object; every other test file using the same generic-helper idiom was
  checked and patches only non-takes-family methods, so needed no change.
  A manual `getattr(db, "list_coach_students", None)` /
  `db.list_coach_students = lambda ...` save-restore pair in
  `test_wave2_be.py` moves to `db.takes`. `patch.object(db,
  "<method>", ...)` sites (including several split across multiple lines,
  which needed a proper multi-line-aware sweep to find) across ten test
  files move to patching `db.takes`. Mock-based
  `database.<method>.return_value` / `.assert_called_once_with` /
  `.call_args` / `.side_effect` sites in `test_lab_recording_persistence.py`
  and `test_recording_transcript_persistence.py` move to
  `database.takes.<method>.*`. Three test files
  (`test_orphan_sweep_and_concurrency.py`, `test_project_tenant_isolation.py`,
  `test_recording_kind.py`) call methods directly on a bare
  `DatabaseService.__new__(DatabaseService)` instance; those direct calls
  move to `.takes.<method>(...)`.
- `scripts/route_fence_baseline.json` — re-frozen (down only):
  `routes/v2/coach.py:v2_coach_students` dropped out of the baseline
  entirely (now within both thresholds) now that its call routes through
  `db.takes`.

No table, migration, route, or response shape changes.

Verified by execution: targeted tests for every touched surface green,
then scripts/local_ci.sh --no-setup green in full (manifest, migration
runner, ruff, mypy, complexity ratchet, unit tier, F1 coverage floor).

Every other entry is untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, takes family batch B (backend)

Re-frozen by the same rule. Two pinned files drift, zero-behavior-change
cleanup: batch B of the takes-family wrapper cleanup (batch A in the
previous entry), the four moderate-traffic methods
(`get_session_intake_context`, `get_session_slide_transcripts`,
`set_session_intake_context`, `set_session_analysis_state`). Four
very-high-traffic methods (`list_user_lab_sessions`,
`v2_list_user_lab_sessions`, `get_arc_sessions`, `v2_get_session`) remain
deferred to their own PR.

- `services/db.py` — 4 takes-family forwarders deleted (66 total; 62 now
  repointed across the recordings, ideal-text and takes-batch-A/B work).
- `routes/v2/coach.py`, `routes/v2/lab_recording.py`,
  `routes/v2/user_sessions.py`, `services/ideal_text_confirmation.py`,
  `services/intake_context.py`, `services/lab_analysis_dispatch.py`,
  `services/lab_recording_persistence.py`, `services/readout_context.py`,
  `services/training_import.py` — call sites repointed to
  `db.takes.<method>()` / `database.takes.<method>()` (only
  `services/pipeline_jobs.py` is itself pinned; the rest changed
  alongside, not listed above — `services/pipeline_jobs.py` had no new
  call sites in this batch but its hash still needed re-freezing after
  batch A, confirmed unchanged by this batch).
- Two hand-rolled test fake DB classes already carrying a `takes`
  property (from batch A) needed no further change; a chained-call
  pattern (`self._svc(cap).set_session_analysis_state(...)`, including a
  backslash-continued multi-line form) in
  `tests/test_analysis_state_broadcast.py` — a factory call followed
  directly by the method, not a simple `obj.method()` — was the one
  genuinely new call shape this batch surfaced; it now reads
  `self._svc(cap).takes.set_session_analysis_state(...)`. Mock-based
  `database.<method>` sites in `tests/test_ideal_text_confirmation.py`,
  `tests/test_readout_context.py`, `tests/test_lab_analysis_dispatch.py`
  and `tests/test_lab_recording_persistence.py` move to
  `database.takes.<method>`. `patch.object(db, "<method>", ...)` sites
  (including several split across multiple lines) in
  `tests/test_intake_context.py`, `tests/test_transcript_edit_route.py`,
  `tests/test_pieces_slide_stickiness.py` and
  `tests/test_processing_jobs.py` move to patching `db.takes` /
  `pipeline_jobs.db.takes`.
- `scripts/route_fence_baseline.json` — re-frozen (down only): three
  `routes/v2/coach.py` functions
  (`v2_coach_archive_training_import`, `v2_coach_restore_training_import`,
  `v2_coach_ab_verdict`) dropped out of the baseline entirely.

No table, migration, route, or response shape changes.

Verified by execution: targeted tests for every touched surface green,
then scripts/local_ci.sh --no-setup green in full (manifest, migration
runner, ruff, mypy, complexity ratchet, unit tier, F1 coverage floor).

Every other entry is untouched.

## Re-freeze 2026-09-16 — post-audit glue cleanup, takes family batch C, closes the family (backend)

Re-frozen by the same rule. Five pinned files drift, zero-behavior-change
cleanup: batch C of the takes-family wrapper cleanup (batches A and B in
the two previous entries) — the four very-high-traffic methods
(`get_arc_sessions`, `v2_get_session`, `list_user_lab_sessions`,
`v2_list_user_lab_sessions`). This closes the takes family (66 of 66
methods repointed) and with it the whole post-audit `services/db.py`
Q-A2 forwarder cleanup across all three families (recordings,
ideal-text, takes).

- `services/db.py` — the last 4 takes-family forwarders deleted. Two
  internal callers on `DatabaseService` itself
  (`v2_get_session_by_id`, a helper inside `v2_cleanup_incomplete_sessions`'s
  neighbourhood computing recorded seconds) now call
  `self.takes.v2_get_session(...)` / `self.takes.v2_list_user_lab_sessions(...)`.
- 26 production files (`routes/v2/arcs.py`, `routes/v2/coach.py`,
  `routes/v2/explore_ideal_text.py`, `routes/v2/user_account.py`,
  `routes/v2/user_sessions.py`, and 21 `services/`/`scripts/` files
  spanning the ideal-text, master-document, voice-confidence, acoustic
  and take-comparison surfaces) — call sites repointed to
  `db.takes.<method>()` / `database.takes.<method>()`. Only
  `services/db.py` and `routes/v2/explore_ideal_text.py` are themselves
  pinned; the rest changed alongside, not listed above.
- Given `get_arc_sessions` is the single most-used takes-family method
  (~45 references), this batch surfaced ~30 hand-rolled test fake DB
  classes implementing it (or `v2_list_user_lab_sessions`) directly on
  themselves; each gained a one-line `takes` property returning `self`
  (added via a small AST-based script that located every class defining
  either method and skipped classes that already carried the property
  from an earlier family's cleanup). `patch.object(db, "<method>", ...)`
  sites (several split across multiple lines) across eleven test files
  move to patching `db.takes`. Mock-based `database.<method>.return_value`
  / `.side_effect` sites in `test_ideal_text_confirmation.py`,
  `test_wave3_be.py` and `test_voice_confidence.py` move to
  `database.takes.<method>.*`. Two `getattr(db, "v2_list_user_lab_sessions",
  ...)` / direct-assignment save-restore pairs
  (`test_wave3_be.py`, `test_slide_deck_capture.py`) move to `db.takes`.
- `scripts/route_fence_baseline.json` — re-frozen (down only): three
  functions (`routes/v2/arcs.py:v2_explore_arc_progress`,
  `routes/v2/coach.py:v2_coach_verify_ideal_text`,
  `routes/v2/coach.py:v2_coach_approve_ideal_text`) dropped out of the
  baseline entirely.

No table, migration, route, or response shape changes.

Verified by execution: targeted tests for every touched surface green
(865 tests), then scripts/local_ci.sh --no-setup green in full
(manifest, migration runner, ruff, mypy, complexity ratchet, unit tier,
F1 coverage floor).

Every other entry is untouched.
