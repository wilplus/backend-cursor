# Job 1 — critical review of the Willab ML provenance audit

Scope: backend-cursor origin/main 87a9309 (manifest 0350), frontend-cursor 6768fbc. Read-only. Eight blockers and the 25 majors in sections A, B, D, E, F, G re-verified against source and a disposable copy of the released schema (willab_confident_moment_head2 plus per-reviewer clones, all dropped). Migrations 0341-0350 and migrations/pending/ read. Six independent reviewers covered the majors; the lead covered the blockers, the migrations and the three sweeps.

## 1. Verdicts

| id | verdict | severity | one-line reason | evidence |
|---|---|---|---|---|
| LEGACY-1 | REPRODUCED | blocker | Promote script writes runtime_config; llm.py serves it on the next request for Take-1 Ideal Text and Say It Stronger; no gate reads MLC2_PROMOTION_ENABLED; table has no RLS, no trigger, service_role INSERT/UPDATE. | slide_selection.py:185 `surface="best_presentation"` ← analysis_worker.py:308 → ideal_text_confirmation.py:105 → ideal_text_block.py:546/309; say_it_stronger.py:264; llm.py:147; ml_surface_contracts.py:127 (60 s cache); grep MLC2_PROMOTION_ENABLED → config.py:227 + readiness only; head2: runtime_config relrowsecurity=f, no triggers, service_role INSERT/UPDATE/DELETE; add_runtime_model_config.sql:15. |
| A-1 | REPRODUCED (one clause overstated) | blocker | Rows serve when the lineage RPC fails; browser ids are uuid5 values that exist in no table; pinned as correct by a test; confirmed live by 0344/0348 headers. Overstated: a lineage-less Take still gets ideal_text_feedback_sets and take_feedback_exposure rows (mislabelled, A-2), so it is distinguishable from never-served. | mlc3_first_client_feedback.py:203-212, :229-247, :482-485 `if lineage is None: visible.append(row); continue`; first_client_repository.py:246-274 `except Exception ... return None`; tests/test_v3_end_to_end_production_shape.py:399-410; feedback_data_contract.py:60-62, :476; migrations 0344/0348 headers (production 2026-09-19/21: write raised, "serves V3 without its audit trail"); ideal_text_changes.py:186 before :215 (claim after replacement). |
| A-3 | NARROWED | major | Default 'off' writes no attempt, hash or snapshot and the route accepts a None boundary; nothing couples V3 serving to enforcement. But the 0348 production quote shows the V3 write passed the Phase-1 liveness join on 2026-09-21, so the web service was enforcing then; residual = fail-open default, no coupling, worker/cron mode unverifiable. | processing_authorization.py:83 default "off", :244 `if not self.enforced: return None`; lab_recording_persistence.py:86; routes/v2/lab_recording.py:427-441; \sf record_feedback_v3_service_candidate_set_v1 lines 93-104 (EXISTS attempt⋈audio else SOURCE_NOT_LIVE); the_candidate_set_writer_resolves_its_own_variable.sql header ("inserted the transcript, the slides ... and then" failed at the count). |
| D-1 | REPRODUCED | blocker | Claim rewrites owner_principal_id on 16 tables incl. evidence_spans and candidate_sets and touches no split table; the release RPC reads the split by the CURRENT owner. | \sf claim_guest_owner lines 88-126; \sf transfer_learning_surfaces_on_owner_claim (presentations + receipts only); \sf create_dataset_release_v1 lines 107-113; dataset_split_assignments UNIQUE(owner_principal_id) append-only; reviewer D reproduced the cross-release case end to end (D-R1 = same mechanism). |
| D-6 | REPRODUCED | blocker | source_cutoff_at appears only in the INSERT column list; no item predicate compares a timestamp, label row, consent or taxonomy. | \sf create_dataset_release_v1: nine RAISEs, none temporal (lines 20-162); `source_cutoff_at` at lines 35/43 only; defined 0296, redefined 0300, nothing after. |
| G-1 | REPRODUCED | blocker | Browser posts to a Next.js route that does not exist; backend route exists; readiness cannot leave 'blocked' without a receipt. | frontend learningExposures.ts:89, :115-121; `find src/app/api -path '*learning*'` → empty; backend routes/v2/learning_exposures.py:21-23; \sf get_seven_surface_readiness_v1 lines 42-43, 177-180. |
| G-2 | REPRODUCED | blocker | No producer exists; the RPC checks none of consent, retention, blindness, quorum; checksums caller-supplied. | grep create_dataset_release_v1/build_dataset_release_manifest in services routes scripts worker.py app.py → docstring only; RAISE list as D-6; `p_manifest->>'manifest_checksum'`, `item_row->>'item_checksum'` inserted verbatim (lines 47, 127). |
| G-3 | NARROWED | blocker (consent) | Consent half holds: the 410 route is the only writer of ml_consent_events/snapshots, so the producer's consent predicate can never be met. Speaker half is wrong as stated: a second live binder exists (record_mlc3_self_speaker_target_v1 via record_mlc3_feedback_self_speaker_target_v1, routes/v2/mlc3_first_client_service.py:290, :657), but it cannot succeed on the released schema (R-2). | routes/v2/mlc2_consent.py:97-99; pg_proc scan of ml_speaker writers → accept_mlc2_founder_consent_v1, record_mlc3_self_speaker_target_v1, register_ml_speaker_principal_v1; tests/test_consent_endpoint.py:388 `__wrapped__()`; take_lifecycle.py:156-168; add_mlc2_confidence_producer_dark_integration.sql:150, :188. |
| A-2 | REPRODUCED | major | At HEAD the claim runs after V3 replacement, so the frozen set holds V3 keys, but the exposure row is labelled with V2's POLICY_VERSION, its candidate_set is the V2 pool snapshot (never contains V3 Confident Voice ids), model/prompt NULL, and the canonical dual-write is skipped for every V3 Take. | ideal_text_changes.py:186, :215, :582, :941-1005 (`policy_version=POLICY_VERSION`), :1041-1054; take_feedback_manager.py:16; take_feedback_candidates.py:226 vs take_feedback_policy_v3.py:568; feedback_data_contract.py:331-352; 0347 adds no policy column; harness: selected ids missing from candidate_set = the relative-confidence keys, canonical RPC called: False. |
| A-4 | NARROWED | minor | Unguarded column, missing FK and recording-blind evidence_hash exist, but the V3 chain verifies attempt↔Take↔snippet recording identity at write time against a UNIQUE immutable attempt row, its evidence re-select is recording-scoped and raises, and no code path repoints a non-null recording_1_id. | add_canonical_feedback_data_contract.sql:79, :98; feedback_data_contract.py:277-291 (hash A == hash B when only recording differs); take_repository.py:395-404; \d processing_recording_attempts `recording_id NOT NULL UNIQUE`, immutable trigger; \sf record_feedback_v3_service_candidate_set_v1 `attempt.recording_id = take_row.recording_1_id`, `evidence->>'recording_id' <> ...` → CANDIDATE_INVALID, re-select raises EVIDENCE_CONFLICT; silent reuse only in V2's record_feedback_exposure_v1. |
| A-6 | REPRODUCED | major | Empty commit becomes literal 'unknown' on every canonical row and NULL on dark frames; nothing rejects it although the repo CHECKs a commit elsewhere. | config.py:196-202; feedback_data_contract.py:64-65, :648, :718; take_feedback_policy_v3.py:883; 0296 DDL lines 28,153,176,264,285 `code_commit TEXT NOT NULL` no CHECK; contrast mlc3_service_activation_risk_decisions `CHECK (production_backend_commit ~ '^[0-9a-f]{40}$')`; experiment with all four env vars unset → '' / 'unknown' / None. |
| A-7 | NARROWED | minor | The served V3 chain does persist detector_version, V3 policy version, manager_rules_version, feature and baseline schema versions, block_partition_version and code_commit; residual = V2 selector/threshold constants stamped on V3 rows, source_code_sha256 never reaches a served table, no version column CHECK-constrained. | take_feedback_policy_v3_service.py:229-230; take_feedback_policy_v3.py:545, :28/:961; \sf record_feedback_v3_service_candidate_set_v1 column list; \d feedback_v3_memberships policy_version CHECK; feedback_data_contract.py:19-21, :693-702; take_feedback_policy_v3.py:873-884, :948-968; ideal_text_changes.py:605. |
| A-10 | NARROWED | major | Frozen Confident Voice score is a function of an unrecorded 5-session baseline with no hash or snapshot on the served chain (impact intact); mis-located (stamp comes from voice_confidence.resolve_confidence_baseline at record time, not delivery_stars) and the live path never re-stamps per Take (only the backfill does). | recording_piece_analysis.py:153-162; voice_confidence.py:239-241, :294-330, :433-441, :171; take_feedback_policy_v3.py:540-548, :592; service:228; experiment: same clip z_sum 1.39 → 0.85 → 0.76 across windows, stamp keys [band, baseline, cues, score, version]; \sf record_feedback_v3_service_candidate_set_v1 has no acoustic_feature_snapshots insert; backfill_voice_confidence.py header. |
| B-1 | NARROWED | major | All cited paths inert in off mode and no boot echo; but production mode is not derivable (ROPA 2026-09-18 predates the 2026-09-20 publish) and off is the documented pre-cutover posture. GET /v2/processing-authorization/status exposes the web mode only. | processing_authorization.py:82-84, :96-97, :161-165, :244-245, :276-277, :291-292, :319-320, :356-357; recording_transcription.py:184-185; routes/v2/coach.py:1565-1568; grep PLF1 in bin/ Procfile nixpacks railpack Dockerfile* → 0; docs/legal/ROPA-ART30.md:40 vs scripts/phase1_policy_publish.sql:1-8. |
| B-2 | REPRODUCED | major | Permit RPC authorizes only the principal and inserts caller-supplied source ids verbatim; clone: one recording attested by two principals. | \sf issue_phase1_provider_permit_v1 (0310, sole definition) line 17, 81-89; \d processing_authorization_snapshots (source ids nullable, no FK, no INSERT trigger); clone: `permit(P2, att7, rec7)` succeeded, snapshots for rec7 name both principals. |
| B-3 | REPRODUCED | major | Resolver freezes the acquisition principal only when the claimed guest already holds a receipt; a receipt-less guest resolves to the claiming account, which then accepts and mints a permit for the guest-era recording. | \sf resolve_phase1_acquisition_principal_v1 lines 12-23 (`AND EXISTS (... receipts WHERE acquisition_principal_id = source)` ... `COALESCE(resolved, p_product_owner_principal_id)`); \sf claim_guest_owner 88-127 touches no processing_* table; routes/v2/processing_authorization.py:146-149 exempts /v2/projects/claim; clone: resolve(P4,U3)=P4, status AUTHORIZED, permit minted for P3-era recording, receipts for P3 = 0. |
| B-4 | REPRODUCED | major | Real orchestrator emits source_relation 'processing_practice_objects'; freeze v4 and mark v1 accept only two relations; nothing written, request stays 'requested'. | data_purge.py:338-358, :413, :556, :691-698; \sf freeze_phase1_purge_inventory_v4 (0313) lines 107-137 ELSE RAISE PURGE_STORAGE_TARGET_SOURCE_INVALID; \sf mark_phase1_storage_object_purged_v1 (0312) 63-64; clone: freeze → SOURCE_INVALID line 131, state 'requested', 0 manifest rows; practice_audio_objects.py:81-82, :135-144 registers in off mode too. |
| B-6 | NARROWED | major | ContextVar loss confirmed (raw thread → (None, None)); live leak is the unconditional say_it_stronger fan-out plus two ungated admin routes; coach_comment_drafter is behind COACH_PREFILL_ENABLED=0, snippet_truncation and conversation_summary thread sites have no callers. | authorized_provider.py:48-50, :77-79; llm.py:113-160; say_it_stronger.py:303-307 ← lab_recording.py:457-482 inside scope :535-541; lab_recording.py:431 `if _coach_prefill_enabled()`; grep apply_extracted_snippets / update_summary_async( → no callers; routes/v2/admin.py:1070-1077, :1390-1441; _CORE_SERVICE_PREFIXES excludes /v2/admin/. |
| B-7 | NARROWED | major | Lane-2 scripts upload subject text with no permit or provider-operation row (real); but the annotation cron cannot run (410) and its export carries hashes + student_id + custom_reason, not snippet text; llm.py does pass provider_operation_ref for chat completions; provider targets fail closed into review_required, not 'done'. | run_openai_preference_finetune.py:47-64, :67-94; grep permit/operation in the four scripts → none; routes/internal_webhooks.py:164-166 + phase2_guard.py:15-28; annotation_export.py:131-146, :171-178; data_purge_registry.py:977-978, :306-307; llm.py:189-195; data_purge.py:483-492 + zero processing_provider_deletion_contracts rows. |
| D-2 | NARROWED | major | Two partition authorities with different keys and formulas (33.3% disagreement) and no cross-check; but nothing reads ml_speaker_split_assignments today and the D4 path fails closed (R-2) rather than double-minting. | dataset_releases.py:54-58 vs \sf assign_ml_speaker_split_v1 lines 24-27; \sf create_dataset_release_v1 lines 56-59, 107-110 (owner table only); pg_proc: only assign_ml_speaker_split_v1 references the speaker table; N=5000 disagree 1664. |
| D-3 | REPRODUCED | major | strategy_version checked only per listed owner against its stored row; items loop checks split only; second bypass: listing the owner with the OLD strategy is also accepted; UNIQUE(owner)+append-only makes a v2 re-assignment impossible. | \sf create_dataset_release_v1 lines 60-66, 111-114; \d dataset_split_assignments; clone: rel-2 (header v2, A omitted) item_count 2 with A under v1; rel-3 (A listed with v1) accepted; rel-4 (A with v2) 'speaker split conflict'. |
| D-5 | REPRODUCED | minor | Same evidence_span_id accepted twice per release and identical audio gets a fresh identity; but under the owner-keyed split the duplicate stays in one partition (double-weighting, not leakage) and the cross-principal clause is D-1's mechanism. | \d dataset_release_items UNIQUE(release_id, item_checksum) only; dataset_releases.py:101-109, :147-148; clone rel-5 `(count 2, distinct evidence 1)`; \d processing_audio_objects UNIQUE(recording_attempt_id), (provider,bucket,key), no sha256 uniqueness; register_exercise_media_object_v1 line 31. |
| D-7 | NARROWED | major | SFT builder takes unedited AI output as target with no guard and the schema stores no model/prompt identity (real), but the builder has no caller, auto_approve_on_send has no writer, and the wired DPO export drops exact-identical pairs; residual = wired DPO lineage lacks generating-model identity and keeps near-identical pairs. | ml_finetuning_export.py:101-103, :123-124, :134, :150-160; db.py:5118-5135; routes/v2/coach.py:2548-2559; ideal_text_annotations.py:266-276; draft_delivery.py:85-87 (zero callers); ml_dpo_export.py:76, :126-128; ml_dpo_release.py:52-77; 0165/0167 DDL. |
| E-1 | NARROWED (re-scoped) | major | Rows ARE distinguishable by Python convention (ai_original_text NULL; reason_chip) with no DB invariant; the coach-only SiS card is unreachable in the shipped FE; but every published SiS card yields an approved_as_is row with created_by=coach although no coach editor exists (live L3 mix, read as "positive acceptance" by phase_a0_diagnostics). | db.py:4946-4957, :5104-5133, :2108-2146; ml_finetuning_export.py:101-103, :126, :134 (executed: coach_only and machine_only both become SFT examples; DPO skips both); routes/v2/coach.py:1015-1054 has no FE caller; lab_recording.py:475 unconditional dispatch; coach_publish_delivery.py:42; phase_a0_diagnostics.py:145-146, :691-692; 0165 DDL no origin/CHECK/trigger. |
| E-2 | REPRODUCED | major | In-place regeneration under an existing coach final reproduced (final_predates_regen=t; plain UPDATE succeeds; no triggers on the three tables); executable trigger is the manual retry of a FAILED job that already carries a coach correction, not routine processing. | db.py:9037-9083 `.upsert(row, on_conflict="snippet_id")`, :9176-9211; analysis_worker.py:290-297; pipeline_jobs.py:637-638 (cleanup only when attempts > 1), db.py:2546/2574; snippet_extraction.py:225-262, :279; pg_trigger: 0 user triggers on moment_suggestions/charisma_snippets/coach_snippet_drafts; clone e2_repro.sql output. |
| E-3 | REPRODUCED | major (contested: reviewer says minor) | Builder and RPC accept a coach_comment_generation item with free wording and no draft linkage end to end on a clone; no predicate joins feedback_revisions or any hash. Reviewer argues minor (dormant lane, constants false, contract names the boundary as "exact evidence span"); kept major as a structural trust gap at the release boundary. | dataset_releases.py:77-116; \sf create_dataset_release_v1 item loop (four RAISEs, then INSERT ... item_payload); clone: builder accepted, RPC returned item_count 1, row stored with `{'coach_text': 'Free coach wording ...'}`; MLC2_DATASET_RELEASES_ENABLED read by readiness only. |
| F-1 | REPRODUCED | major | Both halves executed: PUT#1 200 body carries owner_self_report + machine_value; PUT#2 overwrites the current row; canonical superseding judgment accepted after coach_evidence_comparison_v1. Adjudication rationale corrected: the overwritten row is read live by the Voice Album coach leg, so a post-reveal flip moves Album membership today. | routes/v2/coach.py:3907-3923, :4029-4051; label_quorum.py:456-462; db.py:11218-11227 upsert on (snippet_id, rater_id); no trigger on confidence_labels/label_revision; \sf record_confidence_coach_judgment_v1 (no reveal predicate); voice_album.py:133-146, :214-216; professional_confidence.py:33-40; FE stateRatings.ts:296-303 reads only transcript. |
| F-2 | NARROWED | major | No agreement statistic anywhere; two coaches' conflicting originals coexist (clone); release leg narrowed: dataset_release_items FKs to evidence_spans, not ml_evidence_spans, so ml_judgments rows cannot enter a release by evidence id; inline batches are synthetic_dark. | grep kappa|krippendorff|inter-rater → 0; \d confidence_coach_labels partial UNIQUE (span, rater) WHERE supersedes_id IS NULL; clone: second original accepted, originals on span = 2; \d dataset_release_items FK; \sf prepare_coach_inline_blind_batch_v1 operation_mode synthetic_dark. |
| F-3 | NARROWED | major | Auditor's "both strings in one chain" was hand-passed (the live coach chain is uniform); but confidence_self_reports receives 'feedback-taxonomy-v1' from the Take-review path and 'confidence-owner-five-state-v1' from the V3 service response RPC, no CHECK on coach/peer/self_report/assignment taxonomy, and the RPC accepts taxonomy_versions {}. neutral→IDK is documented design. | routes/v2/coach.py:3099, :3995 + feedback_data_contract.py:19; routes/v2/user_sessions.py:1469; add_mlc3_first_client_service_d2.sql:2612-2619 literal; pg_constraint taxonomy CHECKs list; clone: release with `taxonomy_versions {}` accepted. |
| F-4 | REPRODUCED | major | Take-review path collapses in_between/not_sure→neutral and audio_unclear→unrateable on every confident_voice self-report after 0341; clone accepts both legacy values; second legacy writer exists (PUT confidence-agree, no FE caller). | routes/v2/user_sessions.py:1531-1536; take_feedback_responses.py:8-11; frontend ConfidenceLabelChips.tsx:26-32, DeckChunkModal.tsx:387; db.py:11844-11846; 0341 comment vs CHECK; clone INSERT neutral/unrateable ACCEPTED. |
| G-4 | REPRODUCED | major | Sole Take-creating gate is the founder route check; every takes writer verified (promote RPCs; claim UPDATE only); SQL accepts any owner (clone: non-founder register+promote succeeded); both partitioners split by owner (50 items/1 owner → {'test': 50}); evaluation refuses empty test rows. | lab_recording.py:291-316, :484-489; pipeline_jobs.py:72-92, :135-137; add_recording_attempt_take_boundary.sql:225, :390; dataset_releases.py:47-59, :100, :160-161; confidence_evaluation.py:47-50, :68-70, :145-147, :218-219; tests/test_data_foundation_canary.py (3 passed). |
| G-5 | REPRODUCED | minor | All CHECKs reproduce on a clone and no migration after 0327 or in pending/ widens them; but the schema fails closed, the dark state is declared in the same migration and by the health function, and `trainable` has no runtime reader. Residual = false registry flag + missing registry-vs-vocabulary test. | pg_get_constraintdef on head2 (seven-surface CHECKs; judgment namespace CHECK); add_mlc3_exercise_dark_foundation.sql:19-23, :35-36, :1433; clone: two INSERTs rejected by CHECK; grep trainable readers → none. |
| G-6 | NARROWED | major | Outcome holds (no product path yields a confidence_classification judgment) but the mechanism is mis-located: D5 inline judgment and D5/D6 render acks DO reach submit_/ack_ RPCs; all dead-end because ml_evidence_spans/ml_candidates/ml_candidate_sets are written only by finalize_mlc2_confidence_frame_v1 ← DarkConfidenceWorker, which nothing instantiates. | config.py:235; mlc2_confidence_cutover.py:27-37; take_lifecycle.py:43-45, :156-166; mlc2_confidence_producer.py:222-236; pg_proc writer scan (spans/candidates ← finalize only; judgments ← submit only); \sf prepare_coach_inline_blind_batch_v1 INTO STRICT on ml_evidence_spans; routes/v2/mlc3_first_client_coach.py:197-209; coach_guidance_delivery.py:28-32. |
| MIG-0341..0350 | read | n/a | 0344/0348 are production evidence for A-1 and A-3; 0346/0347/0349 relax V2's three-key rule and route answers through feedback_v3_membership_items but add no policy column (A-2 stands); 0350 and docs/MLC3-GA-ACTIVATION-RUNBOOK.md:3 state MLC-3 reached general availability on 2026-09-20 (claim-level); 0343 moves 16 tables and no split table (D-1 stands); 0345 is a read cache, no learning write. | files listed by manifest number; quoted headers. |
| PENDING | read | n/a | cleanup_retired_sex_data.sql (owner-only two-step), retire_insights_and_strong_sides.sql (destructive, out of manifest by design), seed_phase1_retention_schedule.sql (I-5). None changes a finding. | migrations/pending/README.md and the three files. |

## 2. The 21 adjudicated severity changes

| id | adjudicated | agree? | why (one line) |
|---|---|---|---|
| A-5 | minor | agree | Guard vacuity is real; the exploit is blocked by the purge-request state check on the serving path and by the purge deleting the Take row. |
| A-9 | minor | agree | ml_speaker_principals is at most one per principal; resolution is by join and may be empty, never ambiguous. |
| A-11 | minor, contested | agree | Fields are bound transitively via candidate_set_id/input_hash; the output hash not naming them is hygiene. |
| B-1 | major | agree | Production mode is not derivable from the repo and off is the documented pre-cutover posture; defects are the silent default, no echo, no legacy resolver. |
| B-5 | minor | agree | The purge halts fail-closed on the v2_sessions RESTRICT; the residual is a wrong relation name in the halt and a blind catalog audit. |
| C-1 | kept major | agree | "No caller today" does not close a structural gap; a subject can never be invalidated after release. |
| C-3 | minor | agree | Cron closed by 410; only the CLI exporter reaches the state; export carries hashes and custom_reason, not snippet text (B-7 reviewer). |
| C-4 | minor | agree | The only grant/withdrawal route is 410, so the undrained purge queue is operator-only. |
| C-6 | minor | agree | Three of the six cascades abort on immutable triggers; residual is coach_review_revisions and admin_annotations_log. |
| D-4 | minor | agree | A second split policy needs a migration and a caller change; the missing freeze rule is a design gap to close first. |
| E-1 | kept major | agree, reason corrected | Rows are distinguishable by convention (NULL draft, chip) but with no DB invariant; the live SiS approved_as_is endorsement with no coach editor is the L3 mix. |
| F-1 | major | agree, reason corrected | Not "label store only": the overwritten row feeds the Voice Album coach leg live; blocker would need a live training consumer. |
| F-4 | major | agree | The migration's "no new writes" claim is false on the primary live path; instrument distinctions are collapsed on the way in. |
| FE-1 | kept major | agree | No executable mechanism prevents a render and the backend's own test pins the payload. |
| FE-2 | minor | agree | Backend route is live (J1-1) but the FE shape mismatch keeps the numbers off screen. |
| FE-6 | minor, contested | agree | Server binds the canary to the same literal email; the client short-circuit reproduces the server's answer. |
| FE-7 | minor, contested | agree | Backend treats ai_correct as owner routing only; residual is stale FE contract text and an unused route. |
| H-1 | minor | agree | Promotion runs the candidate through the real serving adapter on golden cases first; residual is the unrecorded prompt-hash binding. |
| I-1 | major | agree | The 410 is policy; the defect is a silently failing provisioned cron with no receipt, no test, stale docs, and a CLI bypass. |
| I-3 | kept major | agree | The CLI exporter is reachable and the run row reports success over a gap. |
| J2-2 | minor | agree | No route calls the two repository methods; a D11 registry and docstring contradiction until a caller is wired. |

Severity changes this review makes on top: A-3 blocker→major; A-4 major→minor; A-7 major→minor; D-5 major→minor; G-5 major→minor; G-3 blocker retained for the consent half only. E-3 contested (kept major).

## 3. New findings

| id | severity | location | scenario | regression test | evidence |
|---|---|---|---|---|---|
| R-1 | major | migrations/add_canonical_feedback_data_contract.sql:2339-2364 (`GRANT ALL ON TABLE public.<table> TO service_role`); add_learning_surface_exposure_receipts.sql:119-120; head2 role_table_grants | Every 0296/0299 canonical table (candidate_sets, feedback_candidates, evidence_spans, confidence_coach_labels, dataset_releases, dataset_release_items, dataset_split_assignments, dataset_exclusions, learning_surface_presentations, learning_surface_exposure_receipts, machine_predictions, generation_runs, transcript_versions, acoustic_feature_snapshots, praise_helpfulness, correction_decisions, confidence_self_reports, feedback_revisions) grants INSERT directly to service_role, which bypasses RLS. Any backend code with the service key can `client.table("dataset_release_items").insert(...)` and skip create_dataset_release_v1's checks; append-only triggers stop UPDATE/DELETE, not INSERT. By contrast all 52 ml_*, feedback_v3_* and feedback_language_* tables have no service_role INSERT grant. | tests/test_canonical_tables_are_rpc_only_postgres.py::test_service_role_cannot_insert_into_canonical_tables | head2 query: 18 tables with service_role INSERT/UPDATE/DELETE; 52 without; `rolbypassrls` for service_role = t; only Python direct access today is a SELECT (ideal_text_repository.py:94). |
| R-2 | major | record_mlc3_self_speaker_target_v1 (add_mlc3_general_user_service_d4.sql; live body lines 146-147 `INSERT INTO public.ml_speakers(id)` / `INSERT INTO public.ml_speaker_principals(speaker_id, acquisition_principal_id)`); add_mlc2_foundation.sql:114-131 (identity_version, identity_hash, created_by, binding_kind, binding_proof_hash, bound_by NOT NULL); routes/v2/mlc3_first_client_service.py:290, :657; first_client_repository.py:160-170 | A general-service user (GA since 2026-09-20 per 0350) confirms "this is my voice"; the principal has no binding (the only other binder is the 410 consent route), so the RPC executes the bare INSERT, violates NOT NULL, rolls back; the repository logs and returns None. No self-speaker assertion, speaker or binding is ever written for anyone the founder path did not bind, so the MLC-3 same-speaker guarantee has no rows behind it. Also confirms G-3's speaker half by accident. | tests/test_mlc3_general_user_service_d4_postgres.py::test_self_speaker_target_binds_a_new_principal_in_the_released_schema (released lane, not the narrow lane whose fixture drops the NOT NULLs, FIX-4) | `BEGIN; INSERT INTO public.ml_speakers(id) VALUES (gen_random_uuid()); ROLLBACK;` → `null value in column "identity_version"`; bare ml_speaker_principals insert → `null value in column "binding_kind"`; only ALTER on ml_speakers is ENABLE ROW LEVEL SECURITY (0302:1453); the D4 postgres test itself does `INSERT INTO ml_speakers(id)` at :809/:1108 against a stub. |
| R-3 | major (structural, no writer today) | services/dataset_releases.py:30-33 (PROVENANCE_KEYS includes "user_self_report", "derived_product_state"), :76-116; add_mlc2_foundation.sql:715-731 ml_judgment_provenance_check | A confidence_classification release item whose label_provenance is {"user_self_report": ...} alone passes the builder and the RPC; an ml_judgments row with actor_provenance='user_self_report', decision 'confident_yes', training_eligibility='potentially_eligible' is accepted by every CHECK (only professional_evaluation is forced to evaluation_only). Owner answers can be declared training supervision by schema, contrary to L3; the sole ml_judgments writer hard-codes blind_coach/blind_peer, so no rows exist today. | tests/test_dataset_releases.py::test_confidence_items_require_a_blind_label; a migration test asserting ml_judgment_provenance_check forces evaluation_only for user_self_report | pg_get_constraintdef output for the five ml_judgments CHECKs; \sf submit_mlc2_confidence_blind_judgment_v1 line 47 provenance CASE, line 119 'potentially_eligible'. |
| R-4 | major | services/ideal_text_changes.py:941, :946, :957-975, :1002-1005; :401-402, :530; tests/test_ideal_text_changes.py:292-295, :396-418 | A student GET on any Take whose ideal_text_feedback_sets row was frozen before #591 (V2's three keys): V3 rows replace self.changes, the #592 guard skips the first branch, the elif re-claims (insert-once) and hands back the V2 row, and `filter_to_selected(V3 rows, V2 keys)` drops every V3 Confident Voice row; if take_index is missing the not_claimable path empties everything. The served bookmarks on those Takes are blanked on every read, and the frozen set never matches what was served. | tests/test_ideal_text_changes.py::test_a_v2_freeze_does_not_blank_the_v3_rows_it_predates (set run.arm_sid = T1; FakeDB claim returns the V2 row; assert the V3 row survives) | harness: elif taken, served V3 rows before/after = 1/0; repo fixture with arm_sid set → `feedback set not claimable ... families=['confident_voice']` → served ids []; existing test passes only because arm_sid == ''. |
| R-5 | major | services/voice_album.py:133-146, :214-216; services/professional_confidence.py:33-40; add_canonical_feedback_data_contract.sql:487, :692, :2357 (voice_album_admissions: only references) | The Album's Coach-Yes leg is `latest_professional_value` sorted by updated_at over the overwritable legacy row (F-1); any coach relabel, including post-reveal, changes membership instantly; the immutable voice_album_admissions table (with confidence_coach_label_id FK) has no writer anywhere, so no record ties an admission to the label that admitted it. | tests/test_voice_album_admission_lineage.py::test_admission_records_the_exact_coach_label_id | grep voice_album_admissions services routes scripts → none; pg_proc scan for INSERT INTO public.voice_album_admissions → empty. |
| R-6 | minor | \sf issue_phase1_provider_permit_v1 lines 81-99; \d processing_authorization_snapshots (source_take_id/source_recording_id nullable, no FK, no INSERT trigger) | An authorized principal calls the permit RPC with a take/recording id that has no processing_recording_attempts row (or does not exist) and receives a permit; the snapshot attests an unregistered recording. Same root as B-2. | tests/test_phase1_processing_postgres.py::test_permit_rejects_unregistered_source_recording | clone: permit returned while the attempt-row query for that recording returned 0 rows. |
| R-7 | minor (blocker if production matches) | \sf resolve_phase1_purge_subject_graph_v1 line 96 (`recording.session_id`), called by resolve_phase1_purge_subject_graph_v2 line 13 and freeze_phase1_purge_inventory_v4 line 23 | On the released rehearsal lane the recordings table has session_v2_id only, so every freeze or subject-graph build raises `column recording.session_id does not exist` before validating a target; the deletion runbook cannot be rehearsed on the lane the repo ships, and if production recordings has the same shape no purge can be frozen for anyone. | tests/test_phase1_deletion_rehearsal_contract.py::test_subject_graph_resolves_on_rehearsal_lane | clone run before stubbing: `ERROR: column recording.session_id does not exist ... HINT: Perhaps you meant "recording.session_v2_id"`. |
| R-8 | minor | routes/v2/coach.py:1501-1513 (re-cut guard probes only coach_snippet_drafts); services/pipeline_jobs.py:600-624 (`_cleanup_before_rerun` deletes coach_snippet_drafts, contradicting db.py:992-994) | A coach's replacement_text_final or star verdict on snippet S is orphaned when a re-cut or crash re-delivery replaces S with S'; no 409, no surfacing. Coach-work loss on an F2 surface, not a corpus breach. | tests/test_recut_guard.py::test_recut_refuses_when_star_finals_or_verdicts_exist | code lines quoted. |
| R-9 | minor | services/ml_dpo_export.py:98-128 (exclusion by raw-text similarity only; reason_chips default None) vs services/db.py:5114-5119 (chip decided on normalized text) | "Great job on that opener" vs "great job on that opener" carries reason_chip approved_as_is; export computes ratio 0.9583 < 0.985 and keeps it as a preference pair with the machine text as the dispreferred side. | tests/test_ml_dpo_loop.py::test_endorsed_rows_are_excluded_by_chip_not_similarity | executed: `similarity=0.9583 | DPO: EXAMPLE`. |
| R-10 | minor | routes/v2/user_sessions.py:1469 + feedback_data_contract.py:19; add_mlc3_first_client_service_d2.sql:2612-2619 | confidence_self_reports receives two taxonomy_version strings for one owner instrument depending on route; any later per-taxonomy filter splits one population by route. Fold into F-3. | tests/test_canonical_self_report_taxonomy.py::test_both_owner_paths_stamp_one_taxonomy | pg_proc: two writers, one literal, one parameter. |
| R-11 | minor | migrations/add_seven_surface_readiness_report.sql:161-163, :242-246 | speaker_disjoint_split.ready = (covered_owners > 0 AND split_ready_owners = covered_owners) is true for a single owner, although one owner can never populate both train and test (G-4); readiness cannot surface the condition it gates. | tests/test_seven_surface_readiness_migration.py (require ≥2 assigned owners or ≥1 per partition) | readiness JSON on head2 emits covered_speaker_count and assigned_owner_count with no floor. |
| R-12 | minor | routes/v2/lab_recording.py:291-316 (only gate); add_recording_attempt_take_boundary.sql:171-245, :340-410; services/mlc2_confidence_readiness.py:83-84 | Founder-only canary scope is enforced solely in the Flask route; the canonical RPCs accept any owner and no readiness invariant counts non-founder Takes, so a script or future route can create non-founder canonical Takes while readiness reports the scope intact. | add a nonfounder_canonical_take_count health invariant and assert readiness blocks when nonzero | clone: register + promote for a fabricated non-founder principal succeeded and appeared in takes. |
| R-13 | minor | services/openai_service.py:41-56 (`openai_copilot_model` / `openai_chat_model` from runtime_config, 60 s cache); add_runtime_model_config.sql:15 | A value written to either key (no repo writer; table service_role-writable and unguarded) is served to every chat/copilot call with no flag, eval or provenance. Same class as LEGACY-1 for non-learned surfaces. | tests/test_runtime_config_keys_are_enumerated.py (allowlist of keys and gated writers) | grep writers of the two keys → none outside phase_a0_diagnostics (read-only). |

Duplicates not listed: D-R1 (same mechanism as D-1), D-R3 (= D-8).

## 4. Unknowns

- Deployed PLF1_PROCESSING_AUTHORIZATION_MODE per Railway service. The 0348 production quote implies web was enforcing on 2026-09-21; worker and crons have no observable surface. Needs boot logs or variables.
- Whether production ml_speakers / ml_speaker_principals match the 0302 DDL (R-2, D-2). Needs `\d` on production and `select count(*) from mlc3_self_speaker_assertions`.
- Whether production recordings has session_id or only session_v2_id (R-7). Needs `\d public.recordings`.
- Whether any openai_surface_model_* / openai_chat_model / openai_copilot_model row exists in production runtime_config (LEGACY-1, R-13). One SELECT.
- Production rollout state (0350 and the GA runbook say generally available since 2026-09-20). Needs `select rollout_state from mlc3_service_rollout_revisions order by revision_number desc limit 1`.
- Whether RAILWAY_GIT_COMMIT_SHA or a fallback is set on web, worker and crons (A-6 proven as code path only).
- Production rows: takes/recording_attempts counts; post-0341 neutral/unrateable rows; approved_as_is counts per field; take_feedback_exposure rows for V3 Takes carrying policy_version 'take-feedback-manager-v2'; Takes whose ideal_text_feedback_sets hold V2's three keys (sizes R-4).
- Whether processing_provider_deletion_contracts and data_retention_rules are registered in production (none in head2).
- Whether the 26 pre-Phase-1 users recorded before enforcement (B-3 population).
- Whether any out-of-repo job calls event_to_openai_messages or log_rlhf_auto_accept_events (repo says none).
- head2 lacks 59 migrations' Supabase base objects and admin_annotation_events; DDL for those was read from migration text, not `\d`.
- hunter-backend docs (A3, D7, runbook §6/§7, review ledger) remain unavailable; nothing here relied on them.
