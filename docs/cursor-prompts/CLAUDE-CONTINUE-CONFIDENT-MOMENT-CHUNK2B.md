# Prompt for Claude — continue Confident Moment Chunk 2B

Continue the MLC-3 Confident Moment Coaching Bundle exactly from the local
handoff. First read, in full:

`/Users/arturwillonski/Documents/hunter-backend/docs/CLAUDE-HANDOFF-CONFIDENT-MOMENT-2026-09-11.md`

Verify its SHA-256 against the value supplied by the founder, then verify every
listed contract, manifest, worktree HEAD, dirty file and checksum. Read
`AGENTS.md`, `CLAUDE.md`, and the WILLAB decision filter before editing.

Immediate task: finish only the two current ML/data P1 corrections in the
uncommitted Chunk 2B worktree:

1. Make coach-update render ACK revalidate the same exact reviewer,
   assignment, blind judgment/reveal/source, membership/deletion and recipient
   authority chain as the secure projection both before and after exposure
   creation. Add both-order reviewer-withdrawal and source-deletion races.
2. Reset and structurally bind every attachment-scoped projection variable so
   mixed confidence/rewrite/praise items cannot inherit coach/render state.
   Add both-order rendered/unrendered mixed-item regressions.

Use `apply_patch`. Preserve all unrelated changes. Do not weaken D11, bypass a
test, add direct reads, number the migration, edit the migration manifest,
commit, push, merge, deploy, activate, collect data, or enable any learning
gate.

After tests pass, freeze exact file checksums and request independent ML/data
and Engineering implementation review. Stop before commit and ask the founder
for explicit commit authorization. Only after both acceptances and that
authorization should Grok receive the exact new base SHA and the narrow Chunk 3
adapter prompt from the handoff.

Return concise progress updates and finish with: changed files and hashes,
tests/results, remaining limitations, gate states, and WILLAB verdict.
