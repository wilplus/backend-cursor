# FE handoff — founder items 2026-07-17 (paste into a fresh Claude session in `frontend-cursor`)

You are working in the willab frontend (Next.js, `/Users/arturwillonski/Documents/frontend-cursor`). The BE side of these items is merged/open as PR #206 in `backend-cursor` — the contracts below are pinned and safe to build against.

Three tokens. **FE-1 is the P0** (a coach cannot publish anything today via the primary path). FE-2 is the founder's new wrap-up screen. FE-3 is small copy hardening.

---

## FE-1 — P0 BUG: "when I click to open the ideal text, nothing happens"

**This is a stacking/DOM-order bug, not a data bug.** The panel mounts, fetches correctly, and renders — it just paints *underneath* the overlay the coach clicked from. All three BFF routes exist and relay fine; the panel has no silent-null branch; every service call soft-fails to visible copy. Ruled out, all of it.

**The mechanism (verified):** in `src/components/willab/Lounge.tsx`, these are direct siblings in one stacking context, in this DOM order:

| line | overlay | z-index |
|---|---|---|
| ~1093 | `BestPresentationOverlay` (hosts `CoachIdealTextPanel`) | `z-40` |
| ~1172 | `StudentRosterOverlay` | `z-30` |
| ~1185 | `StudentDetailOverlay` | `z-40` |
| ~1210 | `CoachReviewOverlay` | `z-40` |

Equal `z-40` + later in DOM ⇒ the source overlay wins. `BestPresentationOverlay` is a full-bleed opaque `bg-background`, so the coach sees the identical screen. **Nothing happens.**

**Proof it's stacking and nothing else:** the *same* button works from the roster path. `StudentRosterOverlay` (`z-30`, line ~86) renders its own nested `StudentDetailOverlay` **inside** itself — a positioned element with non-auto z-index creates a stacking context, trapping the nested `z-40`. So Lounge → roster → student → "Ideal text ready to review" **works**, while Lounge → review-queue bubble → student → the same button **dies**. Only stacking explains that.

**Broken entry points** (both funnel to `setBestPresentationArcId(arcId)`, `Lounge.tsx:154`):
- `StudentDetailOverlay.tsx:184-204` — "Ideal text ready to review" card (dead when opened from the Lounge/review path)
- `CoachReviewOverlay.tsx:292-308` — the wrap-up "The ideal text is ready. Open it…" button

**Why it's fatal, not cosmetic:** the wrap-up's only forward path is that button. A coach who reaches wrap-up via the review queue **has no working route to publish at all**. Only the roster detour works, and nothing signals that.

**Do:** move the `bestPresentationArcId` block **after** `CoachReviewOverlay` in `Lounge.tsx` (preferred — `useBackDismiss`'s LIFO stack at `useBackDismiss.ts:41,68-78` is mount-ordered and already agrees with this). Do **not** just bump to `z-50`: that re-breaks the intent recorded at `Lounge.tsx:1090-1092` (opening a session read *from* the best-presentation overlay).

Then **delete the two now-false comments** — `Lounge.tsx:1215-1216` ("it stacks over the review") and the mirror claim in `StudentDetailOverlay.tsx:182-1184`. They assert the exact thing the code doesn't do; they're what made this bug survive review.

**Secondary symptom, same cause, verify it clears:** `useBackDismiss` keys its LIFO by *mount* order, so the invisible overlay became stack-top and a Back press closed something the coach couldn't see.

**Test:** Lounge → review-queue bubble → student → "Ideal text ready to review" **and** Lounge → review queue → walk to wrap-up → "Open it" — both must paint the panel on top. Re-check the roster path still works (don't trade one for the other). Verify Back closes the ideal-text panel first, returning to the overlay underneath.

---

## FE-2 — The coach's wrap-up screen after the last take (founder)

> "I need a separate page after the last take: instead of the next button, have the **open the ideal text** button; and then when it is reviewed, there should be the last button **PUBLISH**."

**What exists today:** `CoachReviewOverlay.tsx` drives `SnippetScreenShell`; the wrap-up is a virtual last page (`:234` `const isAtWrapup = cursor === session.snippets.length`, total `:233`). The Next button lives in `SnippetScreenShell.tsx:82-95`; `CoachReviewOverlay.tsx:248-259` currently makes it **"Save feedback"** at wrap-up — a per-take draft checkpoint that delivers nothing. Publish is buried inside `CoachIdealTextPanel.tsx:91-121` (`saveAndPublish` = PUT → approve → `publishArc`).

**New BE endpoint — the one read this screen renders from:**

```
GET /api/v2/coach/arc/{arcId}/review-state     → BFF → GET /v2/coach/arc/{arcId}/review-state
```
```jsonc
{
  "arc_id": "…",
  "published": false,
  "takes": [{ "session_id": "…", "take_index": 1, "review_state": "reviewed", "has_reread": true }],
  "takes_saved": 3, "takes_total": 3, "takes_target": 3,
  "ideal": { "assembly_state": "ready", "ready": true, "approved": false,
             "source": "machine", "takes_done": 3 },
  "can_publish": false,
  "blockers": ["IDEAL_TEXT_NOT_APPROVED"],   // and/or "TAKES_NOT_SAVED", "NO_TAKES"
  "pending_session_ids": []
}
```
`review_state` per take: `to_review` → `reviewed` (coach saved it) → `delivered` (published). `can_publish` mirrors **exactly** the preconditions `publish-analysis` enforces, so you render the button's real state instead of discovering it on a failed POST.

**You'll need a new BFF route** at `src/app/api/v2/coach/arc/[arcId]/review-state/route.ts` — clone the existing `ideal-text/route.ts` GET (same auth header, `cache: "no-store"`, try/catch → 502).

**Build the wrap-up as its own screen** (still the virtual last page of the walker is fine — the founder's ask is that it *reads* as a separate page, with no "next"):
1. **Never show "Next"** at wrap-up. The primary action is **"Open the ideal text"** → the same `onOpenArcIdeal(arcId)` (working again after FE-1).
2. Gate that button on `ideal.ready`. When `ideal.assembly_state === "pending"`, show `takes_done`/`takes_target` instead ("assembles after take 3 — N of 3 recorded"). For `"empty"` see FE-3.
3. **PUBLISH is the last button, and only appears once the ideal text is reviewed** — i.e. `ideal.approved === true`. Render it `disabled` with the reason when `can_publish` is false, mapping `blockers`:
   - `TAKES_NOT_SAVED` → "Save each recording's feedback first" (you have `pending_session_ids` to link them)
   - `IDEAL_TEXT_NOT_APPROVED` → "Approve the ideal text first"
4. `published: true` → terminal state ("Delivered"), no publish button.
5. Re-fetch `review-state` after the ideal-text panel closes, so approving there flips PUBLISH live.

Keep `POST /coach/arc/{arcId}/publish-analysis` as the publish call (`arcBatch.ts:22-59` already handles its 409s — they now *shouldn't* fire, but keep the handling; it's the server's own gate).

---

## FE-3 — Ideal-text panel: handle the new `"empty"` state + fix the `null` fallback

Two small holes in `CoachIdealTextPanel.tsx:62-65`:

```ts
const ready = r.assemblyState === "ready" || (r.assemblyState === null && r.text.length > 0);
setPhase(ready ? "ready" : "pending");
```

1. **New BE state `assembly_state: "empty"`** — 3 takes are in but the assembler has nothing to select yet (no coach-confirmed picks). The BE **no longer** serves `"ready"` with an empty block (that was rendering a bare empty textarea = another "dead screen"). Give `"empty"` its own copy — something like *"Nothing to assemble yet — mark some key moments in the takes first."* Today it would fall into the `pending` branch and wrongly say "assembles after take 3" with 3 of 3 recorded.
2. The `assemblyState === null` fallback collapses a legitimate `ready`-with-empty-text into `pending`. Now that the BE always sends `assembly_state`, treat a missing one as a soft error rather than inventing `pending`.

---

## FE-4 — (low priority, BE already unblocked it) retarget the publish relay

Founder hit `Unexpected backend response (HTTP 404)` on "Save and Publish full analysis" — first-ever real click on that button (it was unreachable before FE-1). Cause: the BFF relay `src/app/api/v2/coach/arc/[arcId]/publish/route.ts` still fetches `${backend}/v2/coach/arc/<id>/publish` — the OLD #186 route, deleted BE-side in #195. The BE has restored that path as an **alias** of `publish-analysis`, so publishing works again with zero FE change.

At your own pace: retarget the relay to `${backend}/v2/coach/arc/${id}/publish-analysis` and tell the BE session so the alias can be dropped. While in `arcBatch.ts`: the `409 IDEAL_TEXT_INCOMPLETE` branch is dead (that code was #186's); the live 409s are `TAKES_NOT_SAVED` / `IDEAL_TEXT_NOT_APPROVED`, already rendered via the generic `body.error` path — fine as-is, just don't build on the dead branch.

## FE-5 — Direction chip un-marks after a video upload (founder 2026-07-17)

Coach marks Challenge/Ambiguous/Threat, uploads a breakthrough video → the chip visually resets. The label IS persisted (it round-trips on reopen); the upload flow is clobbering the local `coach_state` with a partial object.

BE affordance shipped: `POST /v2/coach/sessions/<sid>/snippets/<snip>/breakthrough-video` now returns the **authoritative `coach_state`** (`direction_label`, `note`, `tag`, `surfaced`, `breakthrough_video_ref`, `transcript_corrected`) alongside the existing fields — same shape as the per-snippet save echo. Fix: after upload, write `response.coach_state` back into local state (merge, never replace with a partial). If the direction was selected but not yet persisted at upload time, keep the local selection (the echo's `direction_label` may be null in that race — local wins when the echo is null).

## FE-6 — Recorded video auto-uploads (founder: "no need to click the use it button")

In-app MediaRecorder flow: on recording stop, upload immediately — drop the "use it" confirm step. Keep a cancel/re-record affordance during recording; after stop, show upload progress → the attached state. Keep the same `upload_idempotency_key` semantics (same key on retry of the same take, new key on re-record).

## FE-7 — "Show more" acoustic disclosure below the potentiometer

Small toggle button under the ACOUSTIC READ needle on the coach snippet card; expands to the raw per-snippet acoustic data. **Zero BE work — the coach packet already carries everything** on each snippet:

- `features`: `f0_mean`, `f0_sd`, `f0_slope`, `f0_mid_end_delta`, `speech_rate` (wpm) + `speech_rate_pct`, `mean_pause_seconds` (use this, not `mean_pause`), `pause_ratio`, `pause_regularity`, `loudness_range`, `intensity_envelope`, `voiced_ratio`.
- `acoustic_read.baseline`: what the needle was measured against — `"user"` (speaker's own history) / `"parent_take"` (a re-read vs its take) / `"take"` (within-take). Worth a small caption, e.g. "vs your baseline" / "vs the original take".
- `acoustic_read.outside_normal_range`: already rendered as the key-moment hint (if not, it's the one flag worth surfacing in the expanded view).

Coach-only surface — numbers are allowed here (same precedent as the potentiometer itself). Collapsed by default.

## Notes
- BE contracts here are **additive**; nothing you have breaks. `review-state` and `"empty"` are new.
- The BE also fixed the coach's **acoustic-read needle on re-read snippets** (it was pegged dead-centre because a 1–2-piece re-read has no reference to z-score against; it now uses its parent take's distribution). **No FE change** — same `acoustic_read` shape on the snippet. The only visible difference: the `baseline` field can now read `"parent_take"` alongside `"user"`/`"take"`. If you surface that provenance anywhere, add the label; otherwise ignore it.
